# Build & deps
node_modules/
dist/
.astro/
.wrangler/

# Env
.env
.env.*
!.env.example

# Wrangler local secrets (used by `wrangler dev`). The committed example
# `.dev.vars.example` documents the required keys without real values.
.dev.vars
.dev.vars.*
!.dev.vars.example

# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Image pipeline cache
scripts/.image-cache.json

# Reference materials (not part of website)
references/
whatsapp-photo share/
# Source images for the optimize-images pipeline (only the public/img/
# derivatives ship). Kept out of git EXCEPT sources imported directly via
# astro:assets, which must be committed for the build to resolve them.
src/assets/images/*
!src/assets/images/packing/
src/assets/images/packing/*
!src/assets/images/packing/home-packing-service.jpg
website/
wireframe-area-page.html
claudeskills-main/
leadgen-monorepo-master/
partners-pages-copy.md
painless-removals-about-page-FINAL.md
check_png_chunks.ps1

# Playwright e2e artifacts
test-results/
playwright-report/
